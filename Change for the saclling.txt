PhzDataModel/PhotometryGrid => PhotometryCellManager => déclaration of the way grid information are strored in memory 
* add private variables  
       std::vector<std::string>                  m_scaling_filter_name;
       std::vector<double>                       m_differential_scaling;
       
* change the constructors (to add the scaling_filter_name)
* change overload of the factory  (to add the scaling_filter_name)

--- BUILD ---
 
* PhotometryProxy modified to access the scaling from the cell. New function in the PhotometryProxy :
    - scaling_begin(), scaling_end()
    - scaling_cbegin(), scaling_cend()
    - scaling_size()
    - scaling_find(const std::string& scaling_filter)
    in the PhotometryCellManager:  const std::vector<std::string>& scalingFilterNames()
    
--- BUILD ---

* Grid Unit test

--- BUILD ---
    
 
/\ Break compatibility with old grids
* change the grid info /serialization (keeping the old pp in a first step) impact (to be refactored when the luminosity filter is put as ABS MAG):
  - (PhzConfiguration) PhzConfiguration/GridFileHelper.h
  - (PhzConfiguration) src/lib/FilterVariationCoefficientGridOutputConfig.cpp
  - (PhzConfiguration) tests/src/PhotometryGridConfig_test.cpp
  - (PhzConfiguration) tests/src/LuminositySedGroupConfig_test.cpp
  - (PhzConfiguration) tests/src/VolumePriorConfig_test.cpp
  - (PhzConfiguration) tests/src/AxisWeightPriorConfig_test.cpp
  - (PhzConfiguration) tests/src/ModelGridOutputConfig_test.cpp
  
--- BUILD ---
  
TODO  

* change the way the grid serialization

* GridInfo/Serialization Unit test 

* create the configuration for the scaling

* modify the grid creation to add the scaling to the grid

* /\ breaking compatibility /\: put the PP scaling as main scaling and the luminosity with the other scaling. Remove the pp spoofing

* for the CR create a configuration to restrict the available scaling in the output

* for the CR output the reddened ABS MAG

* Integrate change into the UI

